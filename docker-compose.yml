version: '3.9'

services:
  client:
    build:
      context: .
      dockerfile: ./Client/build/Dockerfile
    volumes:
      - ./Client:/usr/src/app
    command: sh -c "npm start"
    ports:
      - "53000:3000"
    stdin_open: true 